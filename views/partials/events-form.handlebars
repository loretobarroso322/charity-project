<!-- views/partials/events-form.handlebars -->

<div class="form-group">
  <label for="title">Charity</label>
  <input name="title" class="form-control" placeholder="Project Red" value="{{event.title}}" />
</div>
<div class="form-group">
  <label for="desc">Description</label>
  <textarea name="desc" class="form-control" placeholder="A description of your event.">{{event.desc}}</textarea>
</div>
<div class="form-group">
  <label for="desc">Donation Amount</label>
  <textarea name="desc" class="form-control" placeholder="money.">{{event.desc}}</textarea>
</div>
